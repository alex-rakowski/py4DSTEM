name: Check code style

on:
  workflow_dispatch::
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]


jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable